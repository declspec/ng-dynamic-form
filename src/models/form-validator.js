function FormValidator(form, validatorFactory) {
    this.form = form;
    this.validatorFactory = validatorFactory;
    this.valid = true;

    this.$$error
}

FormValidator.prototype = {
    attachValidator: function(fieldName, validators, validationCallback) {

    },

    
};